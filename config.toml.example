# Create a folder called `instance` in the root of the repository. Copy
# this file to the `instance` folder. Update the file to reflect your
# environment. Remove `.example` from the filename.

[DATABASE]
host = "localhost"
port = 5432
database = "todo"
username = "todo"
passord = "todo"
